'use client';

import Link from 'next/link';
import { Car, RefreshCw, Package, Shield } from 'lucide-react';

export default function HomePage() {
  return (
    <main className="min-h-screen bg-gray-50">

      {/* ── Hero Section ── */}
      <section className="flex flex-col items-center justify-center text-center px-4 pt-24 pb-16">

        <div className="inline-flex items-center gap-2 bg-white border border-gray-200 rounded-full px-4 py-2 text-sm text-gray-600 shadow-sm mb-8">
          <Car className="w-4 h-4 text-blue-600" />
          Pakistan ka #1 Auto Marketplace
        </div>

        <h1 className="text-4xl md:text-5xl font-bold text-gray-900 leading-tight">
          Apni Dream Car
          <br />
          <span className="text-blue-600">Yahan Milegi</span>
        </h1>

        <p className="text-gray-500 mt-4 text-base md:text-lg max-w-xl">
          New aur used cars, spare parts, trade-in — sab kuch ek jagah
        </p>

        <div className="flex items-center gap-4 mt-8">
          <Link
            href="/cars"
            className="bg-blue-600 text-white px-6 py-3 rounded-xl font-semibold hover:bg-blue-700 transition flex items-center gap-2 shadow-md"
          >
            Cars Dekho →
          </Link>
          <Link
            href="/trade-in"
            className="bg-white text-gray-800 border border-gray-200 px-6 py-3 rounded-xl font-semibold hover:bg-gray-50 transition shadow-sm"
          >
            Car Exchange Karo
          </Link>
        </div>
      </section>

      {/* ── Categories Section ── */}
      <section className="max-w-5xl mx-auto px-4 pb-16">
        <h2 className="text-2xl font-bold text-center text-gray-900 mb-8">
          Kya Dhundh Rahe Ho?
        </h2>

        <div className="grid grid-cols-2 md:grid-cols-4 gap-4">

          <Link href="/cars"
            className="bg-white rounded-2xl border border-gray-200 p-6 flex flex-col items-center gap-3 hover:shadow-md hover:border-blue-200 transition cursor-pointer">
            <div className="w-12 h-12 bg-blue-50 rounded-xl flex items-center justify-center">
              <Car className="w-6 h-6 text-blue-600" />
            </div>
            <p className="font-semibold text-gray-800 text-sm">Cars Khareedo</p>
            <p className="text-xs text-gray-400 text-center">Hazaron cars Pakistan bhar se</p>
          </Link>

          <Link href="/trade-in"
            className="bg-white rounded-2xl border border-gray-200 p-6 flex flex-col items-center gap-3 hover:shadow-md hover:border-green-200 transition cursor-pointer">
            <div className="w-12 h-12 bg-green-50 rounded-xl flex items-center justify-center">
              <RefreshCw className="w-6 h-6 text-green-600" />
            </div>
            <p className="font-semibold text-gray-800 text-sm">Trade-In</p>
            <p className="text-xs text-gray-400 text-center">Apni car exchange karo</p>
          </Link>

          <Link href="/spare-parts"
            className="bg-white rounded-2xl border border-gray-200 p-6 flex flex-col items-center gap-3 hover:shadow-md hover:border-orange-200 transition cursor-pointer">
            <div className="w-12 h-12 bg-orange-50 rounded-xl flex items-center justify-center">
              <Package className="w-6 h-6 text-orange-500" />
            </div>
            <p className="font-semibold text-gray-800 text-sm">Spare Parts</p>
            <p className="text-xs text-gray-400 text-center">Original aur used parts</p>
          </Link>

          <Link href="/showrooms"
            className="bg-white rounded-2xl border border-gray-200 p-6 flex flex-col items-center gap-3 hover:shadow-md hover:border-purple-200 transition cursor-pointer">
            <div className="w-12 h-12 bg-purple-50 rounded-xl flex items-center justify-center">
              <Shield className="w-6 h-6 text-purple-600" />
            </div>
            <p className="font-semibold text-gray-800 text-sm">Verified Dealers</p>
            <p className="text-xs text-gray-400 text-center">Trusted showrooms sirf</p>
          </Link>

        </div>
      </section>

      {/* ── City Section ── */}
      <section className="max-w-5xl mx-auto px-4 pb-20">
        <h2 className="text-2xl font-bold text-center text-gray-900 mb-8">
          Apna Shehar Chuniye
        </h2>

        <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
          {['Karachi', 'Lahore', 'Islamabad', 'Rawalpindi', 'Faisalabad', 'Multan', 'Peshawar', 'Quetta'].map(city => (
            <Link
              key={city}
              href={`/cars?city=${city.toLowerCase()}`}
              className="bg-white border border-gray-200 rounded-xl px-4 py-3 text-center text-sm font-medium text-gray-700 hover:bg-blue-50 hover:border-blue-300 hover:text-blue-700 transition"
            >
              {city}
            </Link>
          ))}
        </div>
      </section>

    </main>
  );
}